# Repo Cleanup Inventory

Generated by cross-referencing all 89 git-tracked files against the actual
CI build log of a clean checkout (run `30301040626`, commit `1581cb7`) — i.e.
what Sphinx genuinely reads, writes, or copies for the live site — plus
`git grep` for cross-links from every page that IS built. Not guesswork:
each item below was individually checked for incoming references before
being placed in its category.

**Ground truth:** the live site currently builds exactly 5 pages —
`content`, `notebooks/appendix`, `notebooks/enclosure_manuf`,
`notebooks/enclosure_model`, `notebooks/trajectories_temp` (temporary) —
plus the `docs/enclose.html` Code API page served via `html_extra_path`.
Everything else tracked in git is either infrastructure or currently unused
by the displayed website.

---

## A. Already decided — just needs `git rm` to finalize (34 files)

These are marked `D` (deleted) in the working tree right now — someone in
an earlier session already deleted them locally, but the deletion was never
committed. No incoming references found anywhere. This is the safest,
highest-confidence category: committing these `git rm`s just finalizes a
decision that already appears to have been made.

- `customary/EnclosureOutline_Oct.md`
- `customary/Intro_ideas.md`
- `customary/Figures/` — all 25 files (africa_worker.jpg, agroec.png, cocoa.jpg,
  Figure1.pptx, monopoly.png(+.bb), nash_eq.png(+.bb), nash_so_comp.png(+.bb),
  not4sale.jpg, not4sale2.jpg, req.png(+.bb), simple.pdf.bb, simple.png(+.bb),
  social_opt_cond.png(+.bb), social_optimum.png(+.bb), social_private.png(+.bb),
  somalia_city.jpg, labintensity.png.bb)
- `extensions/` — all 7 files (costs.md, dwl.md, Edgeworth_add.md, mpl_plot.png,
  partial_security.md, related_models.md, skills_add.md, social_vs_private.md)
- `notes/contest_function.md`
- `notes/plot_eqn_g.png`

The `.bb` files are LaTeX/dvips bounding-box sidecars — build artifacts that
should never have been tracked in the first place, consistent with the
`customary/` directory being an abandoned earlier draft of the material now
superseded by `paper/`.

## B. Newly identified orphans (tracked, present, unused by current site)

Verified via `git grep` — none of these are referenced by any page the site
actually builds.

| File(s) | Why it's orphaned |
|---|---|
| `notebooks/__pycache__/enclose.cpython-37.pyc` | Compiled Python bytecode, tracked by mistake. `__pycache__` is in `.gitignore` now, but this file predates that rule so it wasn't retroactively untracked. Should never be in a repo. |
| `logo.png` | Superseded — `_config.yml` uses `logo2.png`. No reference to `logo.png` anywhere. |
| `notebooks.ipynb` (root level, not in `notebooks/`) | Zero references anywhere. |
| `references.md`, `references.bib` | `references.md` is a real bibliography page (`{bibliography} references.bib`) but it's not in `_toc.yml`, so it's never built/linked. **Judgment call, not a pure delete** — this looks like it could be intentionally revived as a References page for the eventual online appendix rather than removed. `references.bib` (116KB) is presumably also the source for the actual paper's LaTeX bibliography (`paper/references.bib`, if that's the same file or a copy). |
| `notebooks/docs/enclose.html` | Stray duplicate Code API page at the wrong path, left over from a local `pdoc` run with the wrong `-o` target. The real one lives at `docs/enclose.html` (already fixed this session). |
| `notebooks/Figures/` — 16 of 17 files: Boserup_isoquants.png, dmg1.png, dmg3.png, dmg4.png, dmg5.png, dmgfig1.png, Figure1.pptx, labintensity.png, monopoly.png, nash_eq.png, nash_so_comp.png, req.png, simple.png, social_opt_cond.png, social_optimum.png, social_private.png | Not referenced by any built page. (`enclosure_model.ipynb`/`enclosure_manuf.ipynb` generate all their figures live via matplotlib at execution time — these static images are leftovers from an earlier, more manually-illustrated version of the notebooks.) |
| `notebooks/social_opt_cond.png`, `notebooks/social_optimum.png` (root of `notebooks/`, distinct duplicates of the same-named files in `notebooks/Figures/`) | Unreferenced. |
| `notebooks/intro.md` | Not in `_toc.yml`, not cross-linked. |
| `notebooks/enclosure_total.ipynb` | Not in `_toc.yml`, not cross-linked. |
| `notebooks/welfare_comparisons.ipynb` | Not in `_toc.yml`, not cross-linked. |
| `notes/derive te.md` | Not in `_toc.yml`, not cross-linked. |
| `notes/tomake_ghpages.md` | Not in `_toc.yml`, not cross-linked. |

## C. Judgment calls — tracked, current, but not in navigation

These aren't stale — both were updated in this session's "add the mu/tau
extended model" commit and reflect current code. They're just not linked
from `_toc.yml`, so a visitor can't reach them.

- **`notebooks/code.md`** — dropped from `_toc.yml` by a pre-existing local
  edit (kept per your instruction this session). Its content is now largely
  redundant with the direct "Code API" external link already in the Model
  part. Candidate to either drop the file too (finish what that edit
  started) or restore it to nav if you want the `pdoc` command documented
  in-book somewhere.
- **`notebooks/countour.ipynb`** — has real, current content (just
  re-executed against the updated `enclose.py`), but was never in
  `_toc.yml` at all. Either add it to the nav or it's dead weight per your
  own description of the problem.

## D. Not a removal — two things actively broken on the live site right now

Distinct from "unused files," these are **broken references from files that
ARE in the toc**, worth fixing rather than removing:

1. `_toc.yml` links to `notes/partial_exclusive.md` and `notebooks/labor_plot`
   — **neither exists in the tracked repo.** Confirmed via the CI log:
   `WARNING: toctree contains reference to nonexisting document
   'notes/partial_exclusive'` (same for `labor_plot`). These are two dead
   nav entries on the live "Extensions and Ideas" section right now.
2. `notebooks/appendix.md` embeds an image via a Windows-style path
   (`.\Figures\le_mu.png`) that doesn't resolve on Linux CI —
   `WARNING: image file not readable`. The actual file
   (`notebooks/Figures/le_mu.png`) is tracked and legitimately needed; only
   the path syntax is broken (should be `Figures/le_mu.png`).

## E. Keep — actively serving the site or build infrastructure

- `.github/workflows/book.yml`, `.gitignore`, `_config.yml`, `_toc.yml`,
  `requirements.txt`, `README.md`
- `content.md`, `notebooks/appendix.md`, `notebooks/enclosure_model.ipynb`,
  `notebooks/enclosure_manuf.ipynb`, `notebooks/enclose.py`,
  `notebooks/code.md` *(see C)*, `notebooks/trajectories_temp.md`
  *(temporary — already flagged for its own future removal)*,
  `notebooks/generate_trajectories_figure.py`
- `nash_eq.png` (root — embedded in `content.md`), `logo2.png` (book logo),
  `paper/Figures/trajectories.png`
- `docs/enclose.html`, `docs/index.html`, `docs/search.js` (Code API,
  served via `html_extra_path`)
- `notebooks/Figures/le_mu.png` (needed once the path bug in D.2 is fixed)
- `paper/final_revisions.md` (not site content — planning doc)

---

## Suggested next step

Categories A and B (roughly 55 files) could be removed with high
confidence in one commit — nothing currently displayed depends on them.
Category C is two small judgment calls. Category D is two small fixes,
not removals. I haven't acted on any of this — it's an inventory for you
to review before I `git rm` / `git add` anything.
