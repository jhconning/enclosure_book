# Rescued from an orphaned worktree, 2026-07-29

These 14 files came from `.claude/worktrees/confident-moore-ba7adb`, a Claude Code worktree
left behind by a session on the **office machine** — its `.git` pointed at
`C:/Users/jconning/...`, so git on this machine could not resolve it and could not tell us
whether it held uncommitted work.

Of its 89 files, 75 had counterparts elsewhere in the repo (mostly under `notes/customary/`
and `notes/extensions/`, at different paths). **These 14 had no counterpart anywhere**, by
filename, so they were copied out before the worktree was deleted. Relative paths from the
worktree root are preserved.

They are old research material of unknown vintage and have **not** been reviewed for
relevance. Triage before carrying them into the private working repo; several may duplicate
material in `notes/` under different names.

Not rescued, deliberately: `.pyc` files, `settings.local.json`, `logo.png`, and
`notebooks.ipynb` (an index stub).
